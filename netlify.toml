# Netlify configuration for Next.js (Time Track)
# Netlify auto-detects Next.js and uses the OpenNext adapter.
# Set env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, etc.) in Netlify UI.

[build]
  command = "pnpm run build"

[build.environment]
  NODE_VERSION = "20"
